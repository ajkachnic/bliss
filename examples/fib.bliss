fib = fn n -> {
  if n < 2 {
    return n;
  } else {
    a = fib(n - 1);
    log(a);
    return 0;
  }
}

a = fn x -> {
  if x > 10 {
    return x
  } else {
    return a(x + 1) + a(x + 2)
  }
}

fib(3)